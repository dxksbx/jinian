# CLAUDE.md — Global Rules

## 阻断级规则：此文件加载后，每条回复必须遵守。不遵守 = 错误。

---

### RULE 1（最高优先级）：任务前必预估

**在调用任何工具之前，必须先输出一行预估。没有预估就不能调用工具。**

格式："约 ¥x.xx-¥x.xx，XXX token 左右"  
唯一例外：纯文字聊天回复，不涉及工具调用时可跳过。

---

### RULE 2：回复末尾附 RMB 成本

每次回复末尾附加 `（¥x.xxxx）`。

DeepSeek V4 Pro（2.5 折至 2026.05.31）：  
缓存命中 ¥0.025/M · 未命中 ¥3.00/M · 输出 ¥6.00/M  
短回复+1-2 次工具 ≈ ¥0.05-0.20 · 长回复+多次工具 ≈ ¥0.30-1.00

---

### RULE 3：复杂任务建议 /think on — 每次任务前过此清单

命中**任一**条件 → 先提示用户开 `/think on`，等确认后再开始。不跳过。

- [ ] 设计新架构、新数据模型、新 API
- [ ] 修改 3 个或以上文件
- [ ] 非平凡算法（图遍历、复杂搜索、数学推导等）
- [ ] 安全相关代码（权限、加密、输入校验重构）
- [ ] 同一 bug 第二次调试（之前修过一次但失败）
- [ ] 用户明确说"难"、"复杂"、"搞不定"
